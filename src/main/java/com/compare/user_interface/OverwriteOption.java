package com.compare.user_interface;

/**
 * These are the options presented to a user when the output file
 * they specified to write to already exists. They will have the option
 * to overwrite the file, cancel the file write, or append to the file.
 *
 * @author luke
 */
public enum OverwriteOption {
    Yes, No, Append
}
